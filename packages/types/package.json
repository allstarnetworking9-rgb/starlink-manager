{
  "name": "@starlink-manager-pro/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint task for @starlink-manager-pro/types\"",
    "test": "echo \"No test task for @starlink-manager-pro/types\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo \"No format task for @starlink-manager-pro/types\"",
    "clean": "rm -rf dist"
  }
}
