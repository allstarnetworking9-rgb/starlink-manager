{
  "name": "@starlink-manager-pro/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint task for @starlink-manager-pro/ui\"",
    "test": "echo \"No test task for @starlink-manager-pro/ui\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo \"No format task for @starlink-manager-pro/ui\"",
    "clean": "rm -rf dist"
  }
}
